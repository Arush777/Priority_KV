# Copy to .env on each machine. NEVER commit .env.

# Absolute path to this checkout on THIS machine
REPO_ROOT=

# Where large files live (models, datasets, run outputs). Prefer fast local scratch on H200.
PRIORITYKV_SCRATCH=

# Hugging Face (needed to download Qwen3-8B / Gemma)
HF_TOKEN=
HF_HOME=

# CUDA visibility (set on H200 jobs)
# CUDA_VISIBLE_DEVICES=0
