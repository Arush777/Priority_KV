# IR package (`src/ir`)

Neutral scaffolding until friend locks **S1** (research angle).

| Module | Status |
|--------|--------|
| `metrics.py` | Recall@k, MRR, nDCG@k (+ mean over queries) |
| Retrievers / data | Wait for S1 CLAIM |

```bash
PYTHONPATH=src python scripts/test_metrics.py
```
