# W1 FP8 baseline smoke (S1) — FullKV vs FP8 KV in vLLM.
manifest_id: w1_fp8_g0
rev: 1
model:
  local_dirname: Qwen3-8B
  hub_id: Qwen/Qwen3-8B
  revision: b968826d9c46dd6066d109eabc6255188de91218
prompts: data/prompts/w1_fullkv_20.jsonl
decode:
  max_new_tokens: 32
  temperature: 0.0
vllm:
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.45
  max_model_len: 4096
fp8:
  # On-the-fly scale estimation (vLLM). Dataset oneshot is scripts/prep_fp8.py.
  kv_cache_dtype: fp8
  calculate_kv_scales: true
gate:
  # FP8 may diverge more than HF↔vLLM FullKV; still require strong overlap.
  min_exact_match: 0.60
  min_mean_token_agree: 0.80
